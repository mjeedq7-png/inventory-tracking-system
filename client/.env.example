# API URL (optional - defaults to /api which uses Vite proxy in development)
VITE_API_URL=/api

# Production API URL (uncomment and set for production build)
# VITE_API_URL=https://your-api-domain.railway.app/api
