---
description: Create a new API endpoint with tests
---

# Existing API Structure
!`ls -la src/app/api/ 2>/dev/null || ls -la src/pages/api/ 2>/dev/null || echo "no api directory"`

Create API endpoint for: $ARGUMENTS

## Requirements:
1. Create the endpoint following existing patterns
2. Add TypeScript types for request/response
3. Include input validation
4. Add role-based access control
5. Write API tests
6. Update CLAUDE.md if new patterns are introduced

## Verification:
After creating, test the endpoint with curl commands.